## simple-serve
A simple server that can be used to serve a single page or a text or newly added a video

## How to use?
1. Compile using `make` or compile seperately
2. Use Parameter `-filename` if you want to render a file
3. Use a string as a parameter if you want to display text
4. Put the text inside quotes (" ")
5. Use Parameter `-video` if you want to stream a video (only streaming)

## What next?
1. I dunno what to add next, maybe refactor code a little.




